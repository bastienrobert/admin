<% if user_signed_in? %>
  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light mr-auto">
      <%= link_to 'Capsule', 'https://capsule-bordeaux.fr', class: 'navbar-brand', target: '_blank' %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav  mr-auto">
          <li class="nav-item">
            <%= link_to 'Accueil', root_path, class: cp('/dashboard', 'nav-link') %>
          </li>
          <li class="nav-item">
            <%= link_to 'Periodes', periods_path, class: cp(periods_path, 'nav-link') %>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= button_to 'Se déconnecter', destroy_user_session_path, :method => :delete, class: 'btn btn-danger' %>
          </li>
        </ul>
      </div>
    </nav>
  </header>
<% end %>
<% if notice %>
  <div class="alert alert-success" role="alert">
    <%= notice %>
  </div>
<% end %>
